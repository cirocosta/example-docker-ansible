---
- name: 'bootstrap python'
  raw: 'test -e /usr/bin/python || (apt -y update && apt install -y python-minimal)'

- name: 'bootstrap aptitude'
  raw: 'test -e /usr/bin/aptitude || (apt -y update && apt install -y aptitude)'
